from setuptools import setup, find_packages

setup(
    name='MulgaPredict',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'pandas',
        'numpy',
        # 필요한 라이브러리 추가
    ],
)
